pre-commit:
  parallel: false
  commands:
    format-check:
      run: make format
    lint:
      run: make lint
    test:
      run: make test
    archgate:
      run: command -v archgate &>/dev/null && archgate check --staged || echo "archgate not installed, skipping"
      skip:
        - merge
        - rebase
